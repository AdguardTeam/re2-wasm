---
version: 2
deployment:
  name: re2-wasm - deploy
  source-plan: AJL-RE2BUILD
release-naming: ${bamboo.inject.version}
environments:
  - npmjs

npmjs:
  docker:
    image: adguard/node-ssh:21.7--2
  triggers: [ ]
  tasks:
    - checkout:
        force-clean-build: 'true'
    - artifact-download:
        artifacts:
          - name: re2-wasm.tgz
    - script:
        interpreter: SHELL
        scripts:
          - |-
            set -e
            set -x

            # Fix mixed logs
            exec 2>&1

            ls -alt

            # Publish tarball to NPM package registry
            export NPM_TOKEN=${bamboo.npmSecretToken}
            echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
            npm publish re2-wasm.tgz --access public
  requirements:
    - adg-docker: 'true'
  notifications:
    - events:
        - deployment-started-and-finished
      recipients:
        - webhook:
            name: Deploy webhook
            url: http://prod.jirahub.service.eu.consul/v1/webhook/bamboo
